# dependencies
node_modules

# build outputs (auto-generated by build.sh)
out/
dist/
codec.so
platforms/*/static_chafa.node
platforms/*/static_chafa.pdb
platforms/*/*.lib
deps/
*.tgz

# deps source tarballs (auto-fetched, not source)
deps_src/

# caches
.eslintcache
.cache
*.tsbuildinfo

# ide / os
.idea
.DS_Store
